# Production (Vercel / Netlify): public FastAPI URL — no trailing slash
# Required on Vercel — your Render Web Service URL (no trailing slash)
# VITE_API_URL=https://annotra-gzdf.onrender.com

# Local dev: leave unset (defaults to http://127.0.0.1:8765)
